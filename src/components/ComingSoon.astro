---
interface Props { compact?: boolean }
const { compact = false } = Astro.props;
---

<div class:list={['coming-soon', { compact }]}>
  <p class="eyebrow">Coming Soon</p>
  <p>This section is being prepared and will be published when it is ready.</p>
</div>
